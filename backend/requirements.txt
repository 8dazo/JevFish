# ===========================================
# JevFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core framework =============
flask>=3.0.0
flask-cors>=6.0.0
gunicorn>=23.0.0,<24

# ============= LLM =============
openai>=1.0.0

# CAMEL 0.2.78 imports FastMCP from the MCP v1 package layout.
# mcp v2 removed that compatibility path, so keep this bounded until CAMEL is upgraded.
mcp>=1.3.0,<2

# ============= JevFish / TypeSafe System One =============
# Pin the SDK version that is exercised by the live benchmark workflow.
typesafe-sdk==0.7.0

# ============= Zep =============
zep-cloud==3.25.0
httpx>=0.27.0

# ============= OASIS social simulation =============
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= File processing =============
PyMuPDF>=1.24.0
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Utilities =============
python-dotenv>=1.0.0
pydantic>=2.0.0
